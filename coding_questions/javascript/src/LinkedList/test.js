const LinkedList = require("./solution");
const Node = require("./Node");



test("return the correct head", () => {
    let mainList = new LinkedList();
    mainList.addToTail(new Node(2));
    mainList.addToTail(new Node(3));
    expect()

});


test("return the null head", () => {
    let mainList = new LinkedList();
});


test("return the correct tail", () => {
    let mainList = new LinkedList();
});


test("return the null tail", () => {
    let mainList = new LinkedList();
});


test("", () => {
    let mainList = new LinkedList();
});